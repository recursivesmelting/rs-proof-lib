(jbuild_version 1)

(library
 ((name        format_validator)
  (public_name format_validator)
  (inline_tests)
  (libraries (bitstring 
              stdint 
              cryptokit 
              bignum))
  (preprocess (pps (ppx_inline_test ppx_bitstring -no-check ppx_driver.runner)))))